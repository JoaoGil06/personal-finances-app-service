type: object
properties:
  items:
    $ref: "./transactions-items.yaml"
  total:
    type: number
    readOnly: true # Só irá aparecer nas respostas
  limit:
    type: number
    readOnly: true # Só irá aparecer nas respostas
  offset:
    type: number
    readOnly: true # Só irá aparecer nas respostas
