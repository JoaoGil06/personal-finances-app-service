type: object
properties:
  token:
    type: string
    readOnly: true # Só irá aparecer nas respostas
  password:
    type: string
    writeOnly: true # Só irá aparacer nos requests body
  email:
    type: string
    writeOnly: true
